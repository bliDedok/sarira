# Monitoring foundation

Fastify emits request ID, environment, module, action, status, duration, and error code. Sensitive keys are redacted. `@sarira/logger` provides vendor-neutral log and observability ports; no paid service is selected in Phase 2.
